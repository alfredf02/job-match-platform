pluginManagement {
    repositories {
        mavenLocal()
        gradlePluginPortal()
        // Explicit plugin portal mirror to help in restricted networks
        maven { url "https://plugins.gradle.org/m2" }
        mavenCentral()
    }
}

rootProject.name = 'job-service'